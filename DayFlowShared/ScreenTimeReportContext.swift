import DeviceActivity
import SwiftUI

extension DeviceActivityReport.Context {
    /// Shared identifier used by DayFlow and its Device Activity Report extension.
    static let dayFlowSummary = Self("dayflow.summary")
}
